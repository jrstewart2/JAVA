public class IfElse {

    int x;
    int y;
    int z;

    if(x>y){
        // do something
    } else if(y>z){
        // do something else
    } else
        // another something to do

    //TERNARY
    // variable = (condition) ? true:false;
    int time = 20;
    String result = (time <18) ? "Good day" : "Good night";

}
